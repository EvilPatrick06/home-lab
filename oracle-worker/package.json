{
  "name": "oracle-worker",
  "version": "1.0.0",
  "engines": {
    "node": ">=22"
  },
  "description": "Cloudflare Worker proxying the dungeon-scholar Oracle (AI grading/chat) to Groq: injects the server-side API key and enforces Durable-Object rate limiting plus per-request cost clamps.",
  "main": "index.js",
  "engines": { "node": ">=22" },
  "scripts": {
    "check": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "test": "echo \"(oracle-worker) no unit tests yet\" && exit 0",
    "lint": "echo \"(oracle-worker) no lint surface yet\" && exit 0",
    "audit:ci": "npm audit --audit-level=high"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.103.0"
  },
  "overrides": {
    "ws": "^8.21.0",
    "esbuild": "^0.28.1"
  }
}
