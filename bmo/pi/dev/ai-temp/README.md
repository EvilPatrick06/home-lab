# `dev/ai-temp/` — one-off / agent-assisted scripts

**Not** production. Ad-hoc utilities (Discord maintenance, experiments) that should not live next to `scripts/` deploy tooling.

- Prefer env vars over hardcoded IDs; review before run.
- Delete or promote to `scripts/` if something becomes operational.
