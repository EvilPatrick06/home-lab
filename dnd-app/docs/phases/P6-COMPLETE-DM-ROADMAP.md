# P6 Complete-DM Roadmap — AI-DM capability gaps

> Generated 2026-06-04 from a parallel gap-audit workflow (58 agents, adversarially verified).
> 48 confirmed gaps = engine capabilities that EXIST in code but the AI DM cannot drive or perceive.
> Grind one phase = one commit + one release (4-gate at phase end). Mark phases done as shipped.
> Source detail (file:symbol evidence + wire-up) preserved per gap below.

## Already shipped this session (do NOT redo)
- v2.4.41 P6.2a distances | v2.4.42 P6.2b action-economy/reactions VISIBILITY | v2.4.43 P6.2c cover
- v2.4.44 P6.3 request_roll | v2.4.45 P6.4 rulings write-path

## Phase plan

### P6.5 — AI combat action-economy control (stances + spend + hidden)  `[x] shipped v2.4.46`
- **G0** (high) AI DM cannot issue Dash/Disengage/Dodge stance actions on tokens
- **G3** (high) AI cannot trigger action/bonus action/reaction/movement spending in combat turn state
- **G2** (high) AI DM cannot set creatures to Hidden/Stealth state (combat-actions domain)

### P6.6 — AI opportunity attacks + knock-unconscious  `[x] shipped v2.4.47`
- **G1** (high) AI DM cannot make creatures/NPCs take opportunity attacks
- **G4** (high) AI cannot trigger opportunity attacks on enemy creatures despite full engine implementation
- **G5** (medium) AI cannot directly set a creature to Unconscious/0 HP with initialized death saves (requires atomic action)

### P6.7 — AI mounted-combat control + perception  `[x] shipped v2.4.53`
- **G6** (high) AI cannot set or perceive mounted combat state (rider/mount relationship)

### P6.8 — AI creature concentration set/break + auto-break on incap/death  `[x] shipped v2.4.54`
- **G7** (high) AI DM can establish or modify concentration state on creatures in combat
- **G10** (high) Engine applies concentration checks and break logic on damage
- **G12** (high) Concentration breaks on incapacitation/death (automatic triggering when conditions applied or creatures killed)

### P6.9 — AI creature spell-slot tracking + snapshot  `[x] shipped v2.4.55`
- **G8** (high) creature spell slot tracking in combat
- **G13** (high) AI DM can see spell-level spell slots available on enemies in the snapshot

### P6.10 — AI AoE preview/feedback + spell-effect tracking  `[x] shipped v2.4.56`
- **G9** (high) Engine calculates AoE templates (sphere, cone, line, cube, cylinder) and target selection
- **G11** (high) The AI DM cannot trigger, perceive, or manage spell-based effects (Magic Circle, Entangle, Conjure Animals, etc.) with duration/caster/link tracking, despite the engine's architecture supporting entity tracking. Environmental effects (Extreme Cold, etc.) are visible and manageable; spell effects are not.

### P6.11 — AI environment: darkness zones + terrain cells  `[x] shipped v2.4.57`
- **G14** (high) Darkness zones: create/modify regional areas of magical darkness with 'nonmagical'|'darkness'|'deeper-darkness' intensity levels
- **G15** (high) Terrain cells: place difficult terrain, hazardous terrain (fire/acid/pit/spikes), swimming/climbing zones, and portals at map locations

### P6.12 — AI environment: effects + diseases/curses + traps  `[x] shipped v2.4.58`
- **G16** (high) Environmental effects: place active environmental effects (magical hazards, storms, etc.) on the map with mechanical consequences
- **G17** (high) Diseases and curses: apply active diseases and curses to targets with save tracking and mechanical effects
- **G18** (high) Trap placement and triggering: place armed traps on the map and reveal/trigger them during play

### P6.13 — AI encounter loot + treasure tables + magic-item guidance + XP-budget fix  `[x] shipped v2.4.59`
- **G20** (high) Encounter preset loot distribution when using load_encounter action
- **G19** (high) Treasure table data lookup by CR tier and treasure type (individual vs hoard)
- **G22** (high) Magic item rarity and power level guidance for treasure rolls
- **G21** (high) Encounter difficulty rating and XP budget context — data inconsistency in tables

### P6.14 — AI downtime activities + perception  `[x] shipped v2.4.60`
- **G23** (high) Start, progress, and complete downtime activities with resource tracking
- **G26** (high) Perceive party downtime activity progress and active downtime entries

### P6.15 — AI travel/rest: exhaustion + food/water + forced march + rest prereqs  `[x] shipped v2.4.61`
- **G24** (high) Track exhaustion gained from travel, extreme environments, and chases
- **G25** (high) Apply food and water consumption mechanics during travel
- **G27** (high) Track forced march and overland travel exhaustion mechanics
- **G28** (high) Perceive and respect short/long rest prerequisites and completion state

### P6.16 — AI NPC world-state writes: faction/location/secret-motivation  `[x] shipped v2.4.62`
- **G29** (high) Set NPC faction/organization membership
- **G30** (high) Set NPC current location (persistent tracking separate from encounters)
- **G31** (high) Set NPC secret motivation (DM-only hidden goals, drives, fears)

### P6.17 — AI quest log + faction reputation  `[x] shipped v2.4.63`
- **G32** (high) Manage quest/objective log (add, update, complete, track active quests)
- **G33** (high) Manage faction reputation/standing (track PC standing with organizations)

### P6.18 — AI character mutations: condition durations (+perception) + attunement  `[x] shipped v2.4.64`
- **G34** (high) Condition durations on persisted characters
- **G38** (high) Condition duration perception in game snapshots
- **G35** (high) Magic item attunement state mutations

### P6.19 — AI character mutations: equipment equipped + proficiencies  `[x] shipped v2.4.65`
- **G36** (high) Equipment/armor equipped state mutations
- **G37** (high) Armor/equipment proficiency mutations
- **G39** (high) Skill and save proficiency mutations

### P6.20 — AI map: walls + terrain-cell CRUD  `[x] shipped v2.4.66`
- **G40** (high) Wall segment creation, deletion, and modification (solid/door/window/one-way walls, isOpen state, oneWayDirection)
- **G41** (high) Terrain cell creation, deletion, and configuration (terrain type: difficult/hazard/water/climbing/portal, movement cost, hazard damage, portal targets, floor index)

### P6.21 — AI map: drawings + scene regions/triggers  `[x] shipped v2.4.67`
- **G42** (high) Drawing/annotation placement on maps (freehand lines, rectangles, circles, text labels with color, stroke width, visibility control, floor layer support)
- **G43** (high) Scene region/trigger zone creation with spatial shapes (circle/polygon/rectangle), triggers (enter/leave/start-turn/end-turn), and actions (alert-dm, teleport, apply-condition)

### P6.22 — AI map: token elevation/floor + visuals + movement/senses + resist/vuln/immune report  `[x] shipped v2.4.68`
- **G44** (high) Token elevation and floor layer assignment (elevation in feet with falling damage rules, multi-floor map support)
- **G45** (high) Token visual customization (color, border color, border style, label font size, aura with radius/color/opacity/visibility)
- **G46** (high) Token mobility and special movement (swim speed, climb speed, fly speed configuration; special senses: blindsight, tremorsense, truesight with range)
- **G47** (high) Token damage resistance/vulnerability/immunity configuration and reporting

---
## Gap detail (evidence + wire-up)

### G0 [high] <combat-actions> AI DM cannot issue Dash/Disengage/Dodge stance actions on tokens
- **Engine + AI gap:** CONFIRMED REAL GAP: The engine implements Dash/Disengage/Dodge stances in three places: (1) TurnState type in /home/patrick/home-lab/dnd-app/src/renderer/src/types/game-state.ts has isDashing, isDisengaging, isDodging boolean flags; (2) InitiativeSliceState in /home/patrick/home-lab/dnd-app/src/renderer/src/stores/game/types.ts exposes setDashing(entityId), setDisengaging(entityId), setDodging(entityId) methods; (3) the implementations in /home/patrick/home-lab/dnd-app/src/renderer/src/stores/game/initiative-slice.ts lines 543-585 modify turnStates and consume action economies. Players call these via handleAction('dash'/'disengage'/'dodge') in use-game-handlers.ts. However, NO DM action schema exists to trigger these. Verified against: (1) DM_ACTION_SCHEMAS in /home/patrick/home-lab/dnd-app/src/main/ai/ai-schemas.ts contains 60+ actions but none for stances; (2) DmAction union in /home/patrick/home-lab/dnd-app/src/main/ai/dm-actions.ts has no case for stance actions; (3) executeOne() dispatcher in /home/patrick/home-lab/dnd-app/src/renderer/src/services/game-action-executor.ts has no handlers for stance actions; (4) state-snapshot.ts READS stance state in [ACTION ECONOMY] block (lines 100-102) so AI can SEE them but cannot SET them; (5) DM_ACTIONS_SCHEMA_PROMPT in /home/patrick/home-lab/dnd-app/src/main/ai/prompt-sections/dm-actions-schema.ts documents 60+ actions but zero stance actions. The AI can narrate \"the goblin takes the Dodge action\" but has zero engine-level mechanism to enforce it on monster tokens during combat.
- **Wire-up sketch:** Add three DM action schemas + executors: (1) add SetEntityDashSchema/SetEntityDisengageSchema/SetEntityDodgeSchema to ai-schemas.ts DM_ACTION_SCHEMAS map; (2) create action cases 'set_entity_dash'/'set_entity_disengage'/'set_entity_dodge' in dm-actions.ts DmAction union; (3) dispatch them in executeOne() of game-action-executor.ts to call gameStore.setDashing/setDisengaging/setDodging with the matching entityId; (4) document in dm-actions-schema.ts prompt. Examples: {action: 'set_entity_dash', entityLabel: 'Goblin 1'}, {action: 'set_entity_disengage', entityLabel: 'Hobgoblin Warlord'}, {action: 'set_entity_dodge', entityLabel: 'Cult Fanatic'}. Each action consumes the entity's action and sets the corresponding TurnState flag, enforcing reaction/bonus-action rules per 5e PHB 2024 p.74-76 (Dash/Disengage/Dodge actions).

### G1 [high] <combat-actions> AI DM cannot make creatures/NPCs take opportunity attacks
- **Engine + AI gap:** The engine implements full opportunity attack mechanics: checkOpportunityAttack() function in reaction-tracker.ts, integration in use-token-movement.ts, and reaction accounting via useReaction() store action. However, the AI DM has no DM action (no 'opportunity_attack' in ai-schemas.ts DM_ACTION_SCHEMAS or dm-actions.ts union, no case handler in game-action-executor.ts) to trigger enemy opportunity attacks. The AI can see reaction availability in the game state snapshot and reads OA rules in combat-rules.ts prompt, but cannot execute the mechanical OA—only narrate it.
- **Wire-up sketch:** Add a new DM action 'opportunity_attack' (schema in ai-schemas.ts: {action: 'opportunity_attack', attackerLabel: string, targetLabel: string, reason?: string}). Create executor in game-actions/creature-actions.ts that: (1) resolves labels to tokens/entities, (2) calls an attack resolution helper (or uses existing roll_dice + narration), (3) calls gameStore.useReaction(attackerId) to mark reaction spent, (4) applies damage/effects if hit. Update combat-rules.ts to document this action and when to use it. Add contract test in ai-schemas.test.ts.

### G2 [high] <combat-actions> AI DM cannot set creatures to Hidden/Stealth state (combat-actions domain)
- **Engine + AI gap:** The engine has the capability (`TurnState.isHidden` boolean, `setHidden()` setter in initiative-slice.ts, player Hide action wired via use-game-handlers.ts line 278-281), and the AI CAN PERCEIVE hidden state (state-snapshot.ts line 103 exposes `if (ts.isHidden) parts.push('Hidden')` in the [ACTION ECONOMY] block). However, the AI has NO DM ACTION to set creatures to Hidden. Verified: (1) DM_ACTION_SCHEMAS in ai-schemas.ts has no 'set_hidden' or 'hide_entity' action; (2) DmAction union in dm-actions.ts has no hidden-setting action; (3) game-action-executor.ts has no case statement to handle hidden-setting; (4) dm-actions-schema.ts prompt documentation (all 216 lines) lists zero actions for setting creature Hidden state; (5) StatChange union in types.ts has no creature_hidden or similar type. The AI can see isHidden flags in the snapshot and mention Stealth/Hide narrative-wise (as a conditional outcome), but cannot emit an action to actually toggle isHidden on an enemy token. This is a genuine gap: perception exists, engine capability exists, but the AI↔engine contract is broken.
- **Wire-up sketch:** Add a new DM action to allow the AI to set creatures to hidden state. Proposal: new action `set_entity_hidden` with schema `{action: 'set_entity_hidden', entityLabel: string, hidden: boolean, reason?: string}`. Zod schema in ai-schemas.ts, add to DmAction union in dm-actions.ts, document in dm-actions-schema.ts prompt, and add executor case in game-action-executor.ts that calls `gameStore.setHidden(entityId, hidden)` after resolving entityLabel to entityId via the token lookup pattern used by other creature-targeting actions (see creature-actions.ts for precedent).

### G3 [high] <combat-actions> AI cannot trigger action/bonus action/reaction/movement spending in combat turn state
- **Engine + AI gap:** Engine implements TurnState management with useAction, useBonusAction, useReaction, useMovement methods. AI perceives action economy via state snapshot. However, no DM_ACTION_SCHEMAS or StatChange variant exists to let AI spend these resources. AI can read "reaction USED" but cannot make creatures spend actions/reactions/movement.
- **Wire-up sketch:** Add spend-action DM actions: spend_action, spend_bonus_action, spend_reaction, spend_movement (each with entityLabel + amount). Dispatch to game store methods useAction/useBonusAction/useReaction/useMovement. Document in combat-rules.ts that AI can now enforce action economy mechanically.

### G4 [high] <combat-actions> AI cannot trigger opportunity attacks on enemy creatures despite full engine implementation
- **Engine + AI gap:** Engine implements full OA mechanics (reaction-tracker.ts checkOpportunityAttack, attack-resolver.ts resolveAttack) and reaction availability is visible in game state snapshot. AI is instructed to take OAs (combat-rules.ts line 43). But no DM action exists (checked ai-schemas.ts DM_ACTION_SCHEMAS and dm-actions.ts union) and game-action-executor.ts has no case for OA execution. Reactions are player-facing prompts, not AI-commanded. Engine capability exists but AI cannot access it.
- **Wire-up sketch:** Add 'opportunity_attack' DM action schema to ai-schemas.ts with fields: attacker (entity label), target (entity label), weaponName (string, optional for unarmed). Add executor in game-actions/ that resolves via attack-resolver.resolveAttack(). Add case in game-action-executor.ts switch. Route AI instruction (combat-rules.ts line 43) to use this new action via a new DM action that triggers attack resolution.

### G5 [medium] <combat-actions> AI cannot directly set a creature to Unconscious/0 HP with initialized death saves (requires atomic action)
- **Engine + AI gap:** Engine implements full death mechanics (src/renderer/src/services/combat/death-mechanics.ts) and creature state management. However, AI has no DM action or stat change to atomically set creature to 0 HP + Unconscious condition + initialize death saves (0 successes/0 failures). Death saves are character-only mutations (death_save, reset_death_saves use characterName not targetLabel). AI must combine creature_damage + creature_add_condition, or use imprecise creature_damage to 0 HP. No knock_unconscious or creature_initialize_death_saves action exists in DM_ACTION_SCHEMAS or StatChange union.
- **Wire-up sketch:** Add new DM action: { action: 'knock_unconscious', entityLabel: string, reason?: string } that: (1) sets currentHP=0 on token, (2) adds 'Unconscious' condition, (3) initializes deathSaves={successes:0,failures:0} in a new turnStates subfield or via new stat change type 'creature_initialize_death_saves'. Executor in creature-mutations.ts would apply all three atomically. Document in prompt sections with note that this replaces manual damage + condition + stat changes.

### G6 [high] <combat-actions> AI cannot set or perceive mounted combat state (rider/mount relationship)
- **Engine + AI gap:** The engine fully implements mounted combat rules (TurnState.mountedOn and mountType fields, mount-rules.ts enforces controlled mount action restrictions, speed calculations, and forced dismount mechanics). However, the AI DM is blocked from managing mounted state by TWO critical gaps: (1) NO DM actions for mounting/dismounting — the DM_ACTION_SCHEMAS (ai-schemas.ts) and DmAction union (dm-actions.ts) contain no mount_token or dismount_token actions; (2) state-snapshot.ts does NOT expose mountedOn or mountType fields to the AI in the [GAME STATE] block, so the AI cannot perceive which tokens are mounted on which. Mounted combat is currently a UI-only feature (MountModal.tsx) — players click a button to mount/dismount. The AI cannot trigger these mechanics or reason about them.
- **Wire-up sketch:** Add two parallel capabilities: (1) DM actions — new schemas for mount_token and dismount_token in ai-schemas.ts, add to DmAction union in dm-actions.ts, implement executors in game-actions/creature-actions.ts or new mount-actions.ts file; (2) Context exposure — add mounted combat info block to state-snapshot.ts (e.g., "Mounted Entities: - Rider (Label) on Mount (Label, controlled/independent mount type)" and "Mount Relationships: - Mount (Label) has rider Rider (Label)" for clarity). Document new DM actions in dm-actions-schema.ts prompt section with examples.

### G7 [high] <spells-effects> AI DM can establish or modify concentration state on creatures in combat
- **Engine + AI gap:** The engine genuinely implements concentration state management: TurnState.concentratingSpell field exists (game-state.ts:52), is preserved across turns (initiative-slice.ts:171), and has a store setter function (setConcentrating at initiative-slice.ts:600). The AI can SEE concentration state in the snapshot (state-snapshot.ts:75-80 shows a dedicated "Concentration:" section). However, the AI DM has NO mechanism to SET or MODIFY concentration: (1) No DM action schema exists (checked all 45+ actions in ai-schemas.ts and dm-actions.ts), (2) No stat change type exists for concentration (verified against the StatChange union at types.ts:190-238), (3) No handler in the game-action-executor switch statement (game-action-executor.ts:189-376), (4) Prompt sections discuss concentration rules but provide no verb to modify it (character-rules.ts and combat-rules.ts). The AI can narrate concentration breaks and rolls, but cannot programmatically set 'creature X is now concentrating on spell Y' during combat.
- **Wire-up sketch:** Create a new DM action 'set_concentration' with schema {action: 'set_concentration', entityLabel: string, spell: string | undefined, reason: string} in ai-schemas.ts. Add handler in game-action-executor.ts:executeOne() that resolves entityLabel to entityId and calls gameStore.setConcentrating(entityId, spell). Map both setting a spell name and clearing concentration (spell=undefined) to allow breaks. Add test cases in ai-schemas.test.ts to verify the action validates and pairs with execution capability.

### G8 [high] <spells-effects> creature spell slot tracking in combat
- **Engine + AI gap:** The gap is REAL and CONFIRMED. (1) Engine implementation VERIFIED: Character5e type in src/shared/types/character-5e.ts lines 82-83 tracks spell slots with spellSlotLevels and pactMagicSlotLevels properties. formatCharacterForContext in src/main/ai/character-context.ts lines 98-115 reads and displays these slot counts for player characters. (2) AI DM gap VERIFIED: (a) MapToken type in src/renderer/src/types/map.ts (91-174) has NO spell slot properties — creatures on the map do not track slots; (b) ActiveCreatureInfo type in src/main/ai/types.ts (16-23) contains only label, HP, AC, conditions, and monsterStatBlockId — no spell slots; (c) state-snapshot.ts (1-246) never includes spell slot info in the game state snapshot for creatures; (d) No StatChange type for creature spell slot consumption exists. Player characters can use expend_spell_slot and restore_spell_slot (ai-schemas.ts lines 113-124), but only creature-prefixed mutations exist (creature_damage, creature_heal, creature_add_condition, etc. in ai-schemas.ts 207-265), and applyCreatureMutations (creature-mutations.ts 42-94) handles only damage, heal, conditions, and resistances — not spell slots; (e) formatCreatureContext in context-builder.ts (48-104) enriches creatures with CR, resistances, actions, and legendary actions from stat blocks, but never extracts or exposes spellcasting slot counts.
- **Wire-up sketch:** 1. Add spellSlots property to MapToken type (src/renderer/src/types/map.ts): Record<number, {current: number; max: number}>. 2. Add spellSlots to ActiveCreatureInfo (src/main/ai/types.ts). 3. Hydrate spellSlots in state-snapshot.ts formatCreatureContext or formatAvailableMonstersContext for creature spell slots loaded from stat block. 4. Add creature_expend_spell_slot and creature_restore_spell_slot StatChange schemas to ai-schemas.ts (mirror the player versions but with targetLabel). 5. Handle these new types in applyCreatureMutations (creature-mutations.ts) to update token.spellSlots. 6. Extract spellcasting.slots (if present) from statBlock and include in formatCreatureContext output or snapshot.

### G9 [high] <spells-effects> Engine calculates AoE templates (sphere, cone, line, cube, cylinder) and target selection
- **Engine + AI gap:** The engine has full AoE calculation capability (getTokensInAoE, getAffectedCells, findTokensInArea) and the AI can invoke apply_area_effect, BUT the AI has no pre-execution visualization (the human DM gets AoETemplateModal preview; AI does not) and no post-execution feedback (state-snapshot never includes affected targets or AoE outcomes). This violates good UX for tactical spell targeting where the AI should see which targets will be hit before committing the action.
- **Wire-up sketch:** Add an [AoE_PREVIEW] context block to the AI prompt after apply_area_effect actions execute, showing: affected cells (e.g. "cells (10,5) to (15,10)"), affected token labels, and save outcomes. Alternatively, before execution, add a query action that lets AI ask "what tokens are in a 20ft sphere at (12, 8)?" and receive an instant answer, similar to how request_roll lets the AI prompt players for checks.

### G10 [high] <spells-effects> Engine applies concentration checks and break logic on damage
- **Engine + AI gap:** The engine implements concentration check mechanics in concentration-manager.ts (checkConcentrationOnDamage) and death-mechanics.ts (resolveConcentrationCheck). These functions are integrated into the damage application pipeline via use-game-effects.ts, automatically triggering when any token takes damage while concentrating. The AI DM cannot manually trigger this capability—no 'concentration_check' DM action exists in DM_ACTION_SCHEMAS. The AI has read-only visibility of concentrating entities in the snapshot (state-snapshot.ts:73-82) but no triggering mechanism. When the AI narrates environmental damage to a concentrating NPC without emitting a stat change, no concentration check fires. When a stat change is emitted, the check auto-fires. This is a real asymmetry: the capability exists but the AI cannot control it.
- **Wire-up sketch:** Add a 'concentration_check' DM action to DM_ACTION_SCHEMAS (ai-schemas.ts) with schema {action: 'concentration_check', entityLabel: string, damageTaken: number, conSaveModifier?: number, hasWarCaster?: boolean}. Create executor in game-action-executor.ts that calls checkConcentrationOnDamage directly, bypassing the automatic trigger. Document the new action in dm-actions-schema.ts prompt section. This allows the AI to manually trigger concentration checks for any concentrating entity after narrating damage, matching the auto-trigger behavior.

### G11 [high] <spells-effects> The AI DM cannot trigger, perceive, or manage spell-based effects (Magic Circle, Entangle, Conjure Animals, etc.) with duration/caster/link tracking, despite the engine's architecture supporting entity tracking. Environmental effects (Extreme Cold, etc.) are visible and manageable; spell effects are not.
- **Engine + AI gap:** The engine has activeEnvironmentalEffects (displayed in snapshot at state-snapshot.ts:211-217), but NO DM action exists to cast spells or create spell-duration effects. The AI can see environmental effects and has guidance to narrate them (dm-system-prompt.ts:16-22), but lacks the data structure and action types to manage spell-specific effects (caster, duration, spell name, summoned-creature ownership links). Concentration is tracked (state-snapshot.ts:75-82) but only as a name string; ongoing spell effects from that concentration spell are invisible. Summoned creatures appear as regular tokens with no "summoned by / duration" metadata.
- **Wire-up sketch:** Add a spellEffect data structure (name, caster, duration, originCell, affectedCells/tokens, saveDC, conditionIfFail). (1) Extend GameState with activeSpellEffects array. (2) Create DM actions: cast_spell {spellName, caster, targetX?, targetY?, dc?, duration?} and end_spell {spellEffectId}. (3) Extend state-snapshot with [SPELL EFFECTS] block listing active spells by caster/name/duration. (4) Add prompt section on spell-casting mechanics and tracking. (5) Link summoned creatures to summoner via summonedBy/summonDuration fields on token or separate summon registry.

### G12 [high] <spells-effects> Concentration breaks on incapacitation/death (automatic triggering when conditions applied or creatures killed)
- **Engine + AI gap:** The engine implements concentration tracking and damage-based checks (concentration-manager.ts, death-mechanics.ts) with auto-rollback via setConcentrating(). It also documents that concentration breaks on Incapacitated/death in the combat-rules.ts prompt. However, when the AI DM applies Incapacitated via add_entity_condition or kills a creature via creature_kill, there is NO automatic concentration break hook. The system only auto-breaks concentration on damage (via use-game-effects.ts). If an NPC is knocked Unconscious or killed, the AI must manually narrate the concentration break—it cannot trigger it through DM_ACTION_SCHEMAS or StatChange. There is no 'end_concentration', 'break_concentration', or similar action/mutation available to the AI, and no listener on the 'entity:condition-added' event that would auto-fire concentration logic for specific conditions like Incapacitated.
- **Wire-up sketch:** Add a listener in the game-action-executor or use-game-effects.ts hook that detects when 'Incapacitated' (or any condition from the concentration-breaking list: Incapacitated, Paralyzed, Stunned, Petrified, Unconscious) is added to a concentrating creature via add_entity_condition or when a creature's HP is set to 0 via creature_kill. Upon detection, automatically call setConcentrating(entityId, undefined) and optionally emit a summary to the DM alert tray (e.g., "Wiz loses concentration on Haste due to being incapacitated"). This mirrors the existing damage-path logic in use-game-effects.ts:98-126 but triggers on condition addition / death instead of damage.

### G13 [high] <spells-effects> AI DM can see spell-level spell slots available on enemies in the snapshot
- **Engine + AI gap:** MonsterSpellcasting interface defines slots?: Record<string, { slots: number; spells: string[] }> (monster.ts:69), and game state has spellcasting?.slots?: Record<string, number> (game-state.ts:190), but formatCreatureContext() only outputs "Spellcaster (DC X)" without reading or displaying slot counts. No creature_expend_spell_slot or creature_restore_spell_slot mutations exist. No DM action or stat change type enables AI to consume/track creature spell slots. Only player character spell slots are mutable via expend_spell_slot/restore_spell_slot mutations.
- **Wire-up sketch:** Add spellSlotTracking?: Record<string, { current: number; max: number }> to MapToken; include in creature context snapshot (formatCreatureContext) as "Spellcaster (DC X, Slots: 1st(2/4), 2nd(1/3))"; add creature_expend_spell_slot and creature_restore_spell_slot stat mutations with level, count, and optional current-vs-max tracking; add executor cases in creature-mutations.ts to modify token.spellSlotTracking[level].current on apply; document in dm-actions-schema.ts prompt that creature spell usage is tracked and limited.

### G14 [high] <environment-world> Darkness zones: create/modify regional areas of magical darkness with 'nonmagical'|'darkness'|'deeper-darkness' intensity levels
- **Engine + AI gap:** Engine evidence confirmed: DarknessZone interface fully typed in /home/patrick/home-lab/dnd-app/src/renderer/src/types/map.ts (lines 234-241) with id, x, y, radius, floor, and magicLevel fields. Complete CRUD store slice in darkness-zone-slice.ts (addDarknessZone, removeDarknessZone, updateDarknessZone). GameMap interface includes darknessZones array (line 19 of map.ts). AI gap confirmed: (1) Zero DM_ACTION schema for darkness zones in ai-schemas.ts (DM_ACTION_SCHEMAS lines 741-805 - no darkness zone actions); (2) No DmAction union types in dm-actions.ts; (3) Absent from buildGameStateSnapshot() in state-snapshot.ts - AI cannot perceive darkness zones; (4) Not documented in dm-actions-schema.ts prompt section; (5) No StatChange type for darkness zones in types.ts. Result: full engine capability with zero AI DM access.
- **Wire-up sketch:** Add three DM actions: (1) add_darkness_zone: {x, y, radius, magicLevel, floor?} -> calls addDarknessZone; (2) update_darkness_zone: {zoneId, updates: {x?, y?, radius?, magicLevel?, floor?}} -> calls updateDarknessZone; (3) remove_darkness_zone: {zoneId} -> calls removeDarknessZone. Add schemas to ai-schemas.ts DM_ACTION_SCHEMAS map. Add to DmAction union in dm-actions.ts. Add to state-snapshot.ts Environment section to list active darkness zones with coordinates, radius, intensity. Add action reference to dm-actions-schema.ts prompt. Test coverage in ai-schemas.test.ts (contract with executor).

### G15 [high] <environment-world> Terrain cells: place difficult terrain, hazardous terrain (fire/acid/pit/spikes), swimming/climbing zones, and portals at map locations
- **Engine + AI gap:** VERIFIED: The engine implements terrain capabilities fully. TerrainCell interface in /home/patrick/home-lab/dnd-app/src/renderer/src/types/map.ts defines types ('difficult'|'hazard'|'water'|'climbing'|'portal'), hazardType ('fire'|'acid'|'pit'|'spikes'), hazardDamage, and portalTarget. Rendering exists via drawTerrainOverlay in movement-overlay.ts with visual feedback (colors, patterns). Movement calculation respects terrain via getReachableCells and getReachableCellsWithWalls in combat-rules.ts.  However, CONFIRMED GAP IN AI DM: (1) **State snapshot excludes terrain** — buildGameStateSnapshot in state-snapshot.ts never includes activeMap.terrain in the [GAME STATE] block, so AI cannot see what terrain exists on the active map. (2) **No DM action to place/modify terrain** — The DM_ACTION_SCHEMAS in ai-schemas.ts has no 'place_terrain', 'add_terrain', or similar action type, and the game-action-executor.ts switch statement has no terrain executor case. (3) **No terrain mention in prompt section** — dm-actions-schema.ts documents 100+ DM actions but never mentions terrain placement. (4) **AI cannot perceive hazards** — Combat rules section mentions "difficult terrain" theoretically but the AI has zero awareness of actual hazards on the current map.  This is a genuine, complete gap: the capability exists and is fully playable via the map editor UI, but the AI DM has no way to perceive or control it.
- **Wire-up sketch:** To close this gap:  1. **Add terrain to state snapshot** — Modify src/renderer/src/services/game-actions/state-snapshot.ts:    - After the Tokens section, add a "Terrain:" block listing each TerrainCell by position and type    - Format: "- (5,3): difficult terrain (2ft movement cost)" or "- (8,2): fire hazard (1d6 damage on entry)"  2. **Create place_terrain DM action schema** — In src/main/ai/ai-schemas.ts:    - Add PlaceTerrainSchema (action, gridX, gridY, type, movementCost, hazardType?, hazardDamage?, portalTarget?)    - Add to DM_ACTION_SCHEMAS map  3. **Implement terrain executor** — In src/renderer/src/services/game-actions/effect-actions.ts or new terrain-actions.ts:    - executePlaceTerrainCell(action, gameStore, activeMap, stores): mutate activeMap.terrain array via store  4. **Wire executor into dispatcher** — src/renderer/src/services/game-action-executor.ts:    - Import executor function    - Add case 'place_terrain': return executePlaceTerrainCell(...)  5. **Document in prompt** — Update dm-actions-schema.ts Environment section:    - Add: "\`place_terrain\`: {gridX, gridY, type: 'difficult'|'hazard'|'water'|'climbing'|'portal', movementCost, hazardType?, hazardDamage?, portalTarget?}"    - Include usage example  Estimated effort: ~4 hours (schema, executor, wiring, tests, prompt doc).

### G16 [high] <environment-world> Environmental effects: place active environmental effects (magical hazards, storms, etc.) on the map with mechanical consequences
- **Engine + AI gap:** Engine implements addEnvironmentalEffect() and removeEnvironmentalEffect() in effects-slice.ts with full ActiveEnvironmentalEffect and EnvironmentalEffect type support (id, name, category, mechanicalEffect, saveDC). The snapshot shows activeEnvironmentalEffects names only (read-only). However, the AI DM has zero interaction capability: no DM_ACTION schema exists in ai-schemas.ts, no action type in the DmAction union in dm-actions.ts, no executor case in game-action-executor.ts, and no prompt documentation in dm-actions-schema.ts. The AI can perceive active effects but cannot trigger or manage them.
- **Wire-up sketch:** Add to ai-schemas.ts: AddEnvironmentalEffectSchema and RemoveEnvironmentalEffectSchema (with effectId/name, mechanicalEffect details). Add union cases to dm-actions.ts for 'add_environmental_effect' and 'remove_environmental_effect'. Add executor cases in game-action-executor.ts calling new executeAddEnvironmentalEffect() and executeRemoveEnvironmentalEffect() functions. Add DM actions to prompt-sections/dm-actions-schema.ts with examples (fire wall, magical aura, weather hazard). Export addEnvironmentalEffect/removeEnvironmentalEffect from effects-slice in game-store export so executor can call them.

### G17 [high] <environment-world> Diseases and curses: apply active diseases and curses to targets with save tracking and mechanical effects
- **Engine + AI gap:** The gap is CONFIRMED. Part 1 (engine capability): Effects-slice.ts implements addDisease(), addCurse(), updateDisease(), and updateCurse() methods; dm-toolbox.ts defines ActiveDisease and ActiveCurse types with id, targetId, targetName, successCount/failCount; state-snapshot.ts (lines 219-233) reads and displays active diseases/curses in the game snapshot. Part 2 (AI gap): (1) NO DM_ACTION schema exists for applying diseases/curses in ai-schemas.ts (DM_ACTION_SCHEMAS contains 47 actions, none disease/curse-related); (2) NO DM_ACTION discriminated union variant in dm-actions.ts; (3) NO executor case in game-action-executor.ts executeOne() switch (lines 188-377); (4) NO StatChange type for diseases/curses in types.ts (21 variants, none disease/curse-related); (5) NO prompt documentation in prompt-sections/. The AI sees diseases/curses as read-only snapshot data but has ZERO capability to apply, update, or remove them.
- **Wire-up sketch:** Create apply_disease and apply_curse DM_ACTION schemas in ai-schemas.ts (with fields: targetLabel/targetName, diseaseId/curseId or name, optional details like vector or effect). Add corresponding DmAction union variants in dm-actions.ts. Implement executeApplyDisease() and executeApplyCurse() handlers in game-actions/disease-actions.ts, which call gameStore.addDisease()/addCurse() with a generated UUID. Add cases to game-action-executor.ts executeOne() switch. Optionally, add optional StatChange variants (disease_save_success/disease_save_failure) to track save progression beyond the snapshot, or allow updateDisease calls to persist save count changes. Update prompt-sections to document the new actions and snapshot fields.

### G18 [high] <environment-world> Trap placement and triggering: place armed traps on the map and reveal/trigger them during play
- **Engine + AI gap:** Engine implements full trap state management (addPlacedTrap, triggerTrap, revealTrap methods + PlacedTrap type) and exposes armed traps in AI context snapshot. AI can read trap state but has ZERO DM actions (no place_trap, trigger_trap, reveal_trap schemas or executors) and zero stat change types. Traps are read-only from AI perspective despite full engine support for dynamic manipulation.
- **Wire-up sketch:** Add trap DM actions: (1) Define PlaceTrapSchema, TriggerTrapSchema, RevealTrapSchema in ai-schemas.ts with fields {trapId, gridX, gridY}, {trapId}, {trapId}; (2) Add action types to DmAction union in dm-actions.ts; (3) Implement executePlaceTrap, executeTriggerTrap, executeRevealTrap in new file game-actions/trap-actions.ts calling store.addPlacedTrap(), store.triggerTrap(), store.revealTrap(); (4) Wire executors into game-action-executor.ts switch statement; (5) Document actions in dm-actions-schema.ts under new Traps section with example usage; (6) Add trap context to AI system prompt (dm-system-prompt.ts) explaining when/how to use them.

### G19 [high] <encounter-loot> Treasure table data lookup by CR tier and treasure type (individual vs hoard)
- **Engine + AI gap:** ENGINE CONFIRMED: The treasure generation capability is fully implemented in the engine:   - /home/patrick/home-lab/dnd-app/src/renderer/src/components/game/modals/dm-tools/treasure-generator-utils.ts exports generateIndividual() and generateHoard() functions with real implementations that roll treasure based on CR tier.   - /home/patrick/home-lab/dnd-app/src/renderer/public/data/5e/dm/loot-tables/treasure-hoard.json and individual-treasure.json contain 2024 DMG tables with complete CR ranges (0-4, 5-10, 11-16, 17+), coin rolls, gems, art objects, and magic item tables.   - /home/patrick/home-lab/dnd-app/src/renderer/src/services/library/schemas/treasure-tables.schema.ts registers the treasure table schema.   - TreasureGeneratorModal.tsx provides a working UI for DMs to generate treasure.   - The functions handle CR tiers and both individual and hoard treasure types with DMG-compliant fallback tables.  AI DM GAP CONFIRMED: The AI DM has NO access to this capability:   - /home/patrick/home-lab/dnd-app/src/main/ai/ai-schemas.ts: DM_ACTION_SCHEMAS (lines 741-805) does NOT include any treasure generation action (no generate_treasure, roll_treasure, or similar).   - /home/patrick/home-lab/dnd-app/src/main/ai/dm-actions.ts: The DmAction union does NOT include any treasure-related action type.   - /home/patrick/home-lab/dnd-app/src/main/ai/types.ts: StatChange union has NO treasure-related mutations.   - /home/patrick/home-lab/dnd-app/src/main/ai/context-builder.ts: The buildContext() function assembles [CHARACTER DATA], [ACTIVE CREATURES], [WORLD STATE], [CAMPAIGN NOTES], memory context, but NO [TREASURE_TABLES] block.   - /home/patrick/home-lab/dnd-app/src/main/ai/memory-manager.ts: assembleContext() surfaces world state, NPCs, places, rulings, but NO treasure table data.   - /home/patrick/home-lab/dnd-app/src/main/ai/prompt-sections/narrative-rules.ts: Contains only generic text "Follow the DMG treasure tables for level-appropriate rewards" with NO actual table data or API exposed.
- **Wire-up sketch:** 1. Add treasure generation action to DM_ACTION_SCHEMAS and DmAction union:    - Zod schema: GenerateTreasureSchema with fields (crTier: '0-4'|'5-10'|'11-16'|'17+', treasureType: 'individual'|'hoard')    - DmAction type: {action: 'generate_treasure', crTier: CrTier, treasureType: TreasureType}  2. Add executor in game-action-executor.ts:    - Import generateIndividual/generateHoard from treasure-generator-utils    - Load treasure tables via load5eTreasureTables()    - Execute the appropriate function and broadcast result to party via system_message  3. Add treasure table context block in context-builder.ts:    - After loading monster data, also load treasure table metadata (CR tiers available, brief description of each tier's expected loot value ranges)    - Include in buildContext() as [TREASURE_TABLES] section within TOKEN_BUDGETS.srdData budget  4. Update narrative-rules.ts prompt section:    - Add actual treasure generation guidance: "To award treasure, use the [DM_ACTIONS] block with action 'generate_treasure' specifying the CR tier and treasure type (individual hoard); the system will roll from DMG 2024 tables."    - Reference the CR tiers and expected treasure value ranges  5. Schema contract test:    - Update src/main/ai/ai-schemas.test.ts to verify generate_treasure action has both schema and executor coverage

### G20 [high] <encounter-loot> Encounter preset loot distribution when using load_encounter action
- **Engine + AI gap:** This is a confirmed real gap. ENGINE VERIFICATION: (1) The EncounterLoot interface is defined in /home/patrick/home-lab/dnd-app/src/renderer/src/types/encounter.ts with currency and items fields. (2) The Encounter type includes an optional loot field (line 62) that is properly serialized/deserialized. (3) In executeLoadEncounter (/home/patrick/home-lab/dnd-app/src/renderer/src/services/game-actions/creature-actions.ts, lines 625-727), the function loads the preset, extracts preset.monsters, places tokens on the map, and posts a success message—but never accesses or processes preset.loot. The loot field is completely ignored. AI GAP VERIFICATION: (1) DM_ACTION_SCHEMAS in /home/patrick/home-lab/dnd-app/src/main/ai/ai-schemas.ts (line 686-689) shows LoadEncounterSchema accepts only encounterName, with no loot parameter. (2) The StatChange union in /home/patrick/home-lab/dnd-app/src/main/ai/types.ts includes add_item (line 200) and gold (lines 202-208) mutations that the AI can emit to distribute loot to characters. (3) The DM_ACTIONS_SCHEMA_PROMPT in /home/patrick/home-lab/dnd-app/src/main/ai/prompt-sections/dm-actions-schema.ts (line 155-156) documents load_encounter as accepting only {encounterName}, with no guidance on loot distribution. (4) The campaign context in /home/patrick/home-lab/dnd-app/src/main/ai/campaign-context.ts (lines 167-179) surfaces only encounter names and monster lists to the AI, never the loot field. (5) The game state snapshot in /home/patrick/home-lab/dnd-app/src/renderer/src/services/game-actions/state-snapshot.ts does not include any representation of encounter loot or available loot to distribute. CONCLUSION: The engine stores loot data on encounter presets, but (a) executeLoadEncounter never reads or surfaces it, (b) the AI has no DM action to trigger loot distribution, (c) the AI has no context about what loot an encounter contains, and (d) while the AI can manually emit add_item/gold mutations if told the loot, it has no automation or guidance to do so. This is a genuine capability gap: encounter loot is defined and persisted but completely inaccessible to both the game engine (on load) and the AI DM.
- **Wire-up sketch:** To close this gap, you need: (1) **Surface preset.loot in the game state**: When an encounter is loaded, write preset.loot data to gameStore (new store field like encounteredLoot or lootAvailable) so the renderer can display/access it. Alternatively, post a DM chat message listing the loot when the encounter loads. (2) **Document in AI context**: Add the available loot from the loaded encounter to the game state snapshot or a new [ENCOUNTER LOOT] context block so the AI knows what items/currency exist. (3) **Add a distribute_loot DM action** (or extend load_encounter): Either create a new action like {action: 'distribute_loot', items: [...], currency: {...}} with an executor that distributes the loot to character sheets, OR modify load_encounter to optionally include a loot parameter that the executor applies post-placement. (4) **Prompt guidance**: Update the dm-actions-schema.ts prompt to explain when and how to distribute loot after loading an encounter (either manually via add_item/gold stat changes, or via the new distribute_loot action if created). The simplest first step is to surface preset.loot in chat + add [ENCOUNTER LOOT] to the snapshot so the AI sees it and can manually distribute via add_item/gold mutations.

### G21 [high] <encounter-loot> Encounter difficulty rating and XP budget context — data inconsistency in tables
- **Engine + AI gap:** Engine implements encounter budget calculation in two places with inconsistent values: encounter-cr-calculator.ts (renderer) uses level 6 moderate = 1000 XP, while context-builder.ts (main process AI context) uses level 6 moderate = 900 XP. The AI receives and acts on the context-builder values (900) when balancing encounters per the prompt guidance "Balance CR budget", but the UI's EncounterBuilderModal uses the renderer's calculateEncounterDifficulty() function (1000), creating misalignment. The AI has no DM action to adjust encounter budget and cannot perceive which calculation the engine will use at game time.
- **Wire-up sketch:** Root cause: context-builder.ts calculateEncounterBudget() has a hardcoded xpTable that diverges from encounter-cr-calculator.ts XP_BUDGET. Fix: (1) Establish single source of truth for XP budgets (e.g., shared constants file), (2) Update context-builder.ts to import from that source, (3) Add the correct budget values to AI context so it knows which threshold the UI uses, (4) Optional: Add set_encounter_difficulty DM action if dynamic adjustment is needed post-generation. Files to modify: src/main/ai/context-builder.ts (import from shared source), potentially new src/shared/encounter-budgets.ts, and optionally src/main/ai/ai-schemas.ts (add DM action).

### G22 [high] <encounter-loot> Magic item rarity and power level guidance for treasure rolls
- **Engine + AI gap:** Engine implements full treasure table system with rarity-categorized magic items in src/renderer/src/components/game/modals/dm-tools/treasure-generator-utils.ts (rollMagicItemRarity and rollOnMagicItemTable) and public/data/5e/dm/loot-tables/random-magic-items.json (Tables A-I with rarity labels). AI has zero access: no DM action for treasure awarding, no stat change for magic items, no treasure data in state-snapshot.ts, no context-builder path to load TreasureTableData, only generic "follow DMG" guidance with no actual tables. AI must invent items when narrating treasure, violating engine's rarity-balance framework.
- **Wire-up sketch:** Add treasure guidance to AI context: (1) Create new optional 'treasureTables' field in buildContext that loads TreasureTableData from public/data and includes Tables A-I with rarities + sample items per CR tier. (2) Add new DM action 'award_treasure' schema with fields {characterNames, amount, type: 'individual'|'hoard', crTier: '0-4'|'5-10'|'11-16'|'17+'} that triggers treasure generator on renderer side and stat-applies results. (3) Document in dm-actions-schema prompt section with example: "When the party defeats a CR 5 monster, use award_treasure with crTier='5-10' and type='hoard' to roll appropriate loot including magic items guaranteed by rarity." (4) Add prompt guidance: "Treasure rarities must match CR tier — CR 0-4 gives mostly Common/Uncommon, CR 17+ gives Legendary/Artifact." Constraint: load only 3-5 most popular items per table to stay within token budget.

### G23 [high] <exploration-downtime> Start, progress, and complete downtime activities with resource tracking
- **Engine + AI gap:** Engine implements downtime fully (downtime-service.ts: startDowntime, advanceDowntime, calculateDowntimeCost; DowntimeProgressEntry type in campaign.ts). AI DM cannot trigger it: zero DM actions exist (checked DM_ACTION_SCHEMAS, DmAction union, game-action-executor switch, dm-actions-schema.ts prompt docs). Downtime invisible to AI context (state-snapshot.ts omits downtimeProgress; campaign-context.ts and memory-manager.ts silent on downtime). UI path exists (addDowntimeProgress), AI path does not.
- **Wire-up sketch:** 1. Add downtime schemas to ai-schemas.ts: StartDowntimeSchema (activityId, characterId, daysRequired, goldRequired) and AdvanceDowntimeSchema (entryId, days). 2. Add DmAction union variants for start_downtime and advance_downtime. 3. Register both in DM_ACTION_SCHEMAS map. 4. Add executor cases in game-action-executor.ts (executeStartDowntime, executeAdvanceDowntime) dispatching to downtime-service. 5. Expose active downtime entries in state-snapshot.ts (list in-progress activities per character). 6. Add campaign-context.ts section documenting downtime mechanics (activities available, time/gold budgets, completion tracking). 7. Document both actions in dm-actions-schema.ts prompt section with example: place an activity via start_downtime, advance via advance_downtime with day count. 8. Add contract test in ai-schemas.test.ts validating schema ↔ executor parity for both new actions.

### G24 [high] <exploration-downtime> Track exhaustion gained from travel, extreme environments, and chases
- **Engine + AI gap:** Engine implements exhaustion as a persistent condition with numeric levels, displays it to AI via character context, and reduces it after long rest. Exploration rules in dm-system-prompt.ts and exploration-rules.ts define when exhaustion should be applied (extreme cold DC 10, heat DC 5+, altitude DC 10, forced march, chases). However, the AI DM has NO stat change type to APPLY exhaustion to player characters—only reduce_exhaustion exists in the StatChange union. add_condition exists but lacks a value field to track exhaustion levels. This means the AI can see exhaustion in character context and reduce it, but cannot inflict it per the rules it is instructed to follow.
- **Wire-up sketch:** Add `add_exhaustion` stat change type: { type: 'add_exhaustion'; characterName?: string; levels: number; reason: string } to types.ts StatChange union. Add AddExhaustionSchema to ai-schemas.ts. Add case in stat-mutations.ts validateChange (ensure char exists, exhaustion + levels <= 6) and applyChange (find/create Exhaustion condition, set value = Math.min(6, (current?.value ?? 0) + levels)). Document in dm-system-prompt.ts and exploration-rules.ts that add_exhaustion can be used. This allows AI to execute the extreme environment and chase exhaustion mechanics it is told about.

### G25 [high] <exploration-downtime> Apply food and water consumption mechanics during travel
- **Engine + AI gap:** Engine implements party inventory (party-inventory-slice.ts) with item tracking via add_item/remove_item stat changes, and combat-rules.ts documents DMG 2024 malnutrition/dehydration mechanics (Exhaustion penalties for insufficient food/water per day, 5-day starvation auto-death). Exploration-rules.ts documents foraging success grants food/water. However, the AI DM has no stat change type for consumption tracking, no DM action to trigger consumption schedules, and the game-state snapshot does NOT expose party food/water reserves to the AI context. The AI can add food items via add_item, but cannot track consumption during travel, deduct supplies per day, or enforce exhaustion penalties from supply depletion.
- **Wire-up sketch:** Add a new stat change type `consume_supplies` (characterName, supplies: {food_lbs?, water_gal?}, reason) to track party depletion. Add DM actions: `trigger_consumption_check` (entity, consumableType, daysOfTravel) to roll Survival and deduct food/water, and `apply_starvation` (entity, days_without_food) to enforce exhaustion. Include party supplies (total food lbs, total water gal) in game-state snapshot. Update context-builder.ts to expose party supply levels. Document in exploration-rules.ts how to use trigger_consumption_check during multi-day travel with advance_time.

### G26 [high] <exploration-downtime> Perceive party downtime activity progress and active downtime entries
- **Engine + AI gap:** Engine stores campaign.downtimeProgress with full tracking (daysSpent/daysRequired, goldSpent/goldRequired, status). AI context builder does not include downtimeProgress in formatCampaignForContext() despite including 13 other campaign fields. No DM actions, stat changes, or memory-manager sections exist for downtime. AI cannot see which activities are in progress, for which characters, how many days remain, or current progress—and cannot trigger downtime advancement or completion.
- **Wire-up sketch:** Add downtimeProgress field to campaign-context.ts formatCampaignForContext() function (after adventures, before journal). Format as "[DOWNTIME ACTIVITIES]\n- Character: Activity Name (X/Y days, Z/W gp spent, status)" per entry. Optionally: (1) add a stat-change type for advancing downtime days/gold, (2) add DM action to resolve/complete downtime activities, (3) include downtime in memory-manager assembleContext() for campaign continuity. Core fix: surface existing downtime state so AI can narrate milestones and suggest completion/advancement during play.

### G27 [high] <exploration-downtime> Track forced march and overland travel exhaustion mechanics
- **Engine + AI gap:** The engine implements travel pace (set_travel_pace action + inGameTime tracking) and exhaustion (reduce_exhaustion stat change + add_condition for Exhaustion), but the AI DM cannot trigger forced march exhaustion penalties because: (1) game state has no field tracking accumulated hours traveled per day, (2) there is no stat change type for adding exhaustion (only reducing it), (3) the forced march rule (PHB 2024: beyond 8 hours/day causes exhaustion) is not documented in exploration-rules.ts prompt section, and (4) there is no DM action or executor to enforce the 8-hour daily limit. The exploration prompt documents travel paces and extreme environment exhaustion but omits the core forced march mechanic.
- **Wire-up sketch:** 1. Add `accumulatedTravelHours` field to InGameTimeState/TimeSliceState (reset daily or per-session). 2. Create `add_exhaustion` stat change schema (+reduce_exhaustion in types.ts and ai-schemas.ts). 3. Update exploration-rules.ts prompt to document: "Traveling more than 8 hours in a day causes 1 Exhaustion per hour beyond 8 hours." 4. Add executor for add_exhaustion in stat-mutations.ts (apply by adding Exhaustion condition with incremented value). 5. Document accumulated hours in state-snapshot.ts when travelPace is active. 6. AI can now track travel hours via advance_time, check snapshot for hours-so-far, and emit add_exhaustion stat changes when 8-hour threshold is exceeded.

### G28 [high] <exploration-downtime> Perceive and respect short/long rest prerequisites and completion state
- **Engine + AI gap:** CONFIRMED GAP. Engine implementation verified: (1) applyLongRest() in rest-service-5e.ts (line 340) enforces 0 HP check; executeLongRest() in creature-actions.ts (line 568) enforces 24-hour cooldown; restTracking stores lastLongRestSeconds/lastShortRestSeconds. (2) AI perception INCOMPLETE: state-snapshot.ts (lines 185-189) shows ONLY "time since last long rest" in hours—AI sees NO information about: rest availability (24-hour cooldown not expended vs. expended), current HP state (cannot rest at 0 HP), or undisturbed/prerequisites. DM_ACTION_SCHEMAS and dm-actions-schema.ts document short_rest and long_rest actions with no prerequisite warnings. AI cannot perceive whether a rest attempt will fail, nor can it know if characters are at 0 HP during rest narrative. This is asymmetric: the executor enforces these rules silently (returning early without narrative consequence), but the AI has no way to proactively check or narrate failures.
- **Wire-up sketch:** 1. Add rest-availability fields to state-snapshot.ts output: (a) For each character in the snapshot, include canLongRest: boolean based on HP > 0 and (currentSec - lastLongRestSeconds >= 86400 or null). (b) Show hoursUntilLongRestAvailable if cooldown not met. 2. Document rest prerequisites in dm-actions-schema.ts prompt: note that short_rest requires 1 hour uninterrupted, long_rest requires 8 hours uninterrupted + 0 < HP <= max. 3. Add a stat-change type (if needed) or update logic so failed rests post a system message explaining why (0 HP, cooldown not met) so AI sees failure narrative in context.

### G29 [high] <npc-social-quest> Set NPC faction/organization membership
- **Engine + AI gap:** Engine stores faction in NPCPersonality and has setNpcPersonality write path (types.ts:110, memory-manager.ts:324-339). AI has zero DM action schema (ai-schemas.ts DM_ACTION_SCHEMAS), zero union type variant (dm-actions.ts), zero executor (game-action-executor.ts), zero prompt documentation (dm-actions-schema.ts). Faction is excluded from assembleContext NPC personality section (memory-manager.ts:496-508) despite field being present. AI cannot read or write faction.
- **Wire-up sketch:** 1. Add faction field to npc personality section in assembleContext (memory-manager.ts:501-503). 2. Create SetNpcFactionSchema in ai-schemas.ts mirroring set_npc_attitude pattern. 3. Add set_npc_faction to DM_ACTION_SCHEMAS (ai-schemas.ts:741-805). 4. Add set_npc_faction variant to DmAction union (dm-actions.ts:209-225). 5. Add executeSetNpcFaction in game-action-executor.ts (or game-actions/creature-actions.ts) calling window.api.ai.setNpcFaction. 6. Document set_npc_faction in dm-actions-schema.ts:158-159. 7. Contract test will auto-pass (ai-schemas.test.ts enforces schema ↔ executor parity).

### G30 [high] <npc-social-quest> Set NPC current location (persistent tracking separate from encounters)
- **Engine + AI gap:** Engine implements full capability: (1) NPCPersonality.location field exists (types.ts:111); (2) setNpcPersonality() at memory-manager.ts:324-339 persists all fields including location via read-modify-write pattern. However, AI DM cannot trigger it: (1) DmAction union (dm-actions.ts:209-225) has NO set_npc_location action; (2) StatChange union (types.ts:190-238) has NO location mutation type; (3) IPC handlers (ai-handlers.ts) only expose logNpcInteraction and setNpcRelationship, no location setter; (4) DM actions schema (prompt-sections/dm-actions-schema.ts:159) documents only set_npc_attitude for NPC tracking; (5) assembleContext (memory-manager.ts:497-508) exposes NPCPersonality fields but NOT location. The engine can persist but the AI cannot command it.
- **Wire-up sketch:** Add set_npc_location DM action: (1) Add `{ action: 'set_npc_location'; npcName: string; location: string }` to DmAction union (dm-actions.ts); (2) Add ZodSchema validation in ai-schemas.ts DM_ACTION_SCHEMAS; (3) Add IPC handler in ai-handlers.ts to call memMgr.setNpcPersonality() with updated location; (4) Add IPC channel in ipc-channels.ts (AI_SET_NPC_LOCATION); (5) Expose NPCPersonality.location in assembleContext [NPC PERSONALITIES] section so AI knows current locations and can update them; (6) Document set_npc_location in prompt-sections/dm-actions-schema.ts; (7) Add executor in game-action-executor.ts (executeSetNpcLocation stub/no-op since persisted server-side only).

### G31 [high] <npc-social-quest> Set NPC secret motivation (DM-only hidden goals, drives, fears)
- **Engine + AI gap:** The engine genuinely implements this capability AND the AI DM genuinely has NO way to set or perceive it. CONFIRMED: (1) Engine implementation exists: NPCPersonality.secretMotivation field is defined at src/main/ai/types.ts:112, and setNpcPersonality at src/main/ai/memory-manager.ts:324-338 accepts all NPCPersonality fields including secretMotivation. (2) AI cannot set it: DM_ACTION_SCHEMAS (src/main/ai/ai-schemas.ts:741-805) has no schema for setting NPC personality, motivation, or secrets. The only NPC actions are set_npc_attitude (line 799) which only sets attitude/reason, and set_npc_relationship (line 801) which only sets relationships. No DM action exists to trigger setNpcPersonality. (3) AI cannot perceive it: assembleContext at src/main/ai/memory-manager.ts:496-508 only exposes personality, voiceNotes, and lastInteractionSummary—never secretMotivation. The prompt sections (src/main/ai/prompt-sections/dm-actions-schema.ts, line 159) only document set_npc_attitude. This violates the intended DM-only contract: if the AI could set secrets, it would see them, defeating secrecy. The gap is REAL because there's no bridge between AI command and engine storage.
- **Wire-up sketch:** 1. Add SetNpcSecretMotivationSchema to ai-schemas.ts: schema for action 'set_npc_secret_motivation' with fields {action, npcName, secretMotivation} 2. Add to DM_ACTION_SCHEMAS record: 'set_npc_secret_motivation': SetNpcSecretMotivationSchema 3. Add executor case in game-action-executor.ts (creature-actions.ts): executeSetNpcSecretMotivation() calls gameStore.updateSidebarEntry or similar, or better: intercepts the DmAction and calls memory manager's setNpcPersonality with the motivation (keep it out of sidebar, in memory-only). The memory manager already handles this via setNpcPersonality(). 4. Document in dm-actions-schema.ts prompt section under NPC Tracking: 'set_npc_secret_motivation': {npcName, secretMotivation} — "hidden motivation only you know". 5. Do NOT expose secretMotivation in assembleContext; it should never be sent to the AI in read operations. The action write-only flow keeps secrets opaque.

### G32 [high] <npc-social-quest> Manage quest/objective log (add, update, complete, track active quests)
- **Engine + AI gap:** Engine implements quest persistence via WorldStateSummary.activeQuests and setWorldStateSummary(). Memory manager exposes activeQuests in [WORLD SUMMARY] context block for AI to read. However, zero DM actions exist to modify quests: no add_quest, update_quest, complete_quest, or remove_quest in DM_ACTION_SCHEMAS or DmAction union. The only write path (add_journal_entry) is narrative-only, not structured quest tracking. AI is read-only for this domain.
- **Wire-up sketch:** Create a new DM action schema (e.g., update_quest_log) with variants for: add_quest(name, description), complete_quest(name), update_quest(name, description), remove_quest(name). Add to DM_ACTION_SCHEMAS in ai-schemas.ts. Implement executor case in game-action-executor.ts to call a new game-action (quest-actions.ts) that mutates gameStore.worldStateSummary.activeQuests and persists via memory manager. Document in prompt-sections/narrative-rules.ts or social-rules.ts with examples.

### G33 [high] <npc-social-quest> Manage faction reputation/standing (track PC standing with organizations)
- **Engine + AI gap:** NPCPersonality.faction field exists in types.ts (line 110) for storing faction membership, and SidebarEntry.attitude tracks individual NPC attitudes. However, there is no faction reputation structure, no DM action to modify faction standings, no stat change type, and crucially the `faction` field is NOT surfaced to the AI in assembleContext() (memory-manager.ts lines 496-508 explicitly exclude it from NPC personality context). The engine has the seed (faction string field) but no reputation tracker, and the AI has zero visibility into faction data.
- **Wire-up sketch:** To implement: (1) Create FactionReputation interface in memory-manager.ts with fields {factionName: string, partyStanding: number, lastModified: string}; (2) Add methods to MemoryManager: getFactionReputation(factionName), setFactionReputation(factionName, standing), adjustFactionReputation(factionName, delta); (3) Add DM action adjust_faction_standing {factionName, delta, reason} to dm-actions.ts and ai-schemas.ts; (4) Surface faction standings in assembleContext() alongside NPC personalities (e.g., "[FACTION STANDINGS] Thieves Guild: +15, City Watch: -5"); (5) Optionally add stat change type {type: 'faction_standing', faction: string, delta: number, reason: string} if DM wants to record via STAT_CHANGES blocks.

### G34 [high] <character-mutations> Condition durations on persisted characters
- **Engine + AI gap:** Engine supports durations on EntityCondition (tokens) via add_entity_condition DM action with duration parameter, but ActiveCondition (persisted characters) has no duration field in the schema, no duration parameter in add_condition stat change, and no duration visibility in state snapshot. The AI DM can set timed conditions on game tokens but not on persisted player characters, despite both using the same game-state condition system.
- **Wire-up sketch:** 1. Extend ActiveCondition in src/shared/types/character-common.ts to include `duration?: number | 'permanent'` 2. Update StatChange union in src/main/ai/types.ts: add duration parameter to add_condition/remove_condition 3. Update Zod schema in src/main/ai/ai-schemas.ts: AddConditionSchema and RemoveConditionSchema to include optional duration 4. Update stat-mutations.ts (line 223): include duration when creating conditions 5. Update state-snapshot.ts (line 143): append duration to condition context display so AI sees timed vs permanent 6. Verify persisted character conditions are read/checked against durations elsewhere in the game loop (if needed)

### G35 [high] <character-mutations> Magic item attunement state mutations
- **Engine + AI gap:** CONFIRMED GAP. Engine implementation verified: (1) Character5eState in src/shared/types/character-5e.ts:39 has `magicItemAttuned?: Record<string, boolean>` state map. (2) Chat commands in src/renderer/src/services/chat-commands/commands-player-inventory.ts (lines 91-168, attune/unattune) directly modify `state.magicItemAttuned[instanceId]` and persist via saveCharacter(). Engine fully supports toggling attunement state. AI gap confirmed: (1) StatChange union in src/main/ai/types.ts has 30 types (damage, heal, add_item, grant_feature, etc.) but NO attune/unattune variant. (2) DM_ACTION_SCHEMAS in src/main/ai/ai-schemas.ts has 44 actions but NO attunement action. (3) Character context in src/main/ai/character-context.ts (lines 1-227) includes spells, weapons, armor, features, resources, but NO attunement field — the state.magicItemAttuned map is never extracted or exposed. (4) No grep results for attune/magicItem in AI context files or prompt sections (except one DMG spell reference). The AI DM cannot see attunement state and has no mechanism to trigger mutations.
- **Wire-up sketch:** 1. Add StatChange type in src/main/ai/types.ts: `{ type: 'attune_item'; characterName?: string; itemName: string; reason: string }` + `{ type: 'unattune_item'; characterName?: string; itemName: string; reason: string }`. 2. Add Zod schemas in src/main/ai/ai-schemas.ts: AttuneMagicItemSchema + UnattuneSchema. Add to DM_ACTION_SCHEMAS? (optional: could use stat changes alone, or add attune DM action for symmetry with equip/unequip). 3. Add stat-mutation executor in src/main/ai/stat-mutations.ts to apply attune_item/unattune_item: find character by name, find magic item by name (using getEffectiveMagicItems + instance ID lookup), toggle character.state.magicItemAttuned[instanceId]. 4. Extract attunement in character context (src/main/ai/character-context.ts): add section reading character.state?.magicItemAttuned, cross-reference with character.magicItems to show "Magic Items (Attuned): item1, item2 / Available: item3, item4". 5. Document in prompt sections (src/main/ai/prompt-sections/) that attune_item/unattune_item exist and explain the 3-item attunement limit. 6. Contract test: update src/main/ai/ai-schemas.test.ts to verify both stat change and action can round-trip if action is added.

### G36 [high] <character-mutations> Equipment/armor equipped state mutations
- **Engine + AI gap:** Engine implements equipped state tracking (EquipmentItem.equipped and ArmorEntry.equipped fields), displays equipped armor in character context (AC calculation), and persists the state. However, the AI DM has no StatChange type or DM action for equipping/unequipping items or armor, no mutation executor case to apply such changes, and no visibility into unequipped equipment the character carries. The AI can read equipped state but cannot trigger mutations to change it.
- **Wire-up sketch:** Add equip_item and equip_armor StatChange types to types.ts; add validation/application logic to stat-mutations.ts applyChange() and validateChange(); add corresponding Zod schemas to ai-schemas.ts; update StatChangeSchema union; and optionally expand character-context.ts to display unequipped equipment/armor so AI can see what's available to equip.

### G37 [high] <character-mutations> Armor/equipment proficiency mutations
- **Engine + AI gap:** Engine implements Proficiencies5e (weapons/armor/tools/languages/savingThrows arrays) as persistent character data displayed to the AI via character-context.ts. However, no StatChange type exists for granting/revoking these proficiencies. The union at types.ts:190-239 and ai-schemas.ts:272-303 omits any proficiency-mutation cases. grant_feature/revoke_feature only mutate the features array, not the proficiencies object. No DM action provides a control path either.
- **Wire-up sketch:** Add proficiency stat-change types to the union (src/main/ai/types.ts and ai-schemas.ts): grant_weapon_proficiency, revoke_weapon_proficiency, grant_armor_proficiency, revoke_armor_proficiency, grant_tool_proficiency, revoke_tool_proficiency (each with characterName, proficiency name, reason). Implement validation and applyChange arms in stat-mutations.ts that mutate char.proficiencies[type].push/splice. Document in prompt sections so the AI DM can use them.

### G38 [high] <character-mutations> Condition duration perception in game snapshots
- **Engine + AI gap:** VERIFIED AS A REAL GAP. (1) Engine evidence: EntityCondition interface (src/renderer/src/types/game-state.ts:135) includes `duration: number | 'permanent'` field. Game store fully tracks conditions with durations in gameStore.conditions. DM action `add_entity_condition` (src/main/ai/dm-actions.ts:134) supports optional `duration` parameter. The executor in creature-conditions.ts line 25 and 111 honors the duration when adding conditions. (2) AI gap confirmed: The game state snapshot (src/renderer/src/services/game-actions/state-snapshot.ts:142-144) renders conditions WITHOUT displaying the duration field. Line 143 shows: `${c.entityName}: ${c.condition}${c.value ? ` ${c.value}` : ''} (${c.source})` — notice duration is completely absent. The snapshot displays c.entityName, c.condition, c.value, and c.source, but NOT c.duration (which exists on the EntityCondition type). Additionally, character-level add_condition StatChange mutations (ai-schemas.ts:90-94 and types.ts:194) do NOT include a duration parameter at all—the AI DM cannot even specify condition duration for player characters. For tokens (DM actions), duration is supported in the prompt (line 94 of dm-actions-schema.ts), but the AI cannot perceive remaining time in the snapshot.
- **Wire-up sketch:** Fix requires two parts: (1) Enhance state-snapshot.ts line 143 to display duration info (e.g., duration rounds remaining if numeric, or 'permanent' if set), calculating remaining rounds from appliedRound if in combat. (2) Add duration field to add_condition StatChange schema (ai-schemas.ts:90-94 and types.ts:194), with mutation logic in stat-mutations.ts to apply the duration to character conditions (which will require extending the character condition type to carry duration, since currently only tokens track it via gameStore.conditions). Document duration in dm-actions-schema.ts prompt section.

### G39 [high] <character-mutations> Skill and save proficiency mutations
- **Engine + AI gap:** ADVERSARIALLY VERIFIED. The engine genuinely implements skill and save proficiency state (confirmed in character-5e.ts lines 235-240: SkillProficiency5e with proficient/expertise booleans; Proficiencies5e.savingThrows array at line 232). The AI DM genuinely has ZERO capability to modify these fields:  (1) NO StatChange type for skill/save proficiencies: The StatChange union (src/main/ai/types.ts) contains 30+ mutations (damage, heal, conditions, items, ability scores, features, exhaustion, etc.) but no grant_skill_proficiency, revoke_skill_proficiency, grant_save_proficiency, or revoke_save_proficiency variants.  (2) NO DM action for proficiencies: DM_ACTION_SCHEMAS (ai-schemas.ts lines 741-805) lists 50+ actions but none targeting proficiencies.  (3) NO stat-mutations executor: The applyChange switch statement (stat-mutations.ts lines 194-399) handles all StatChange types; proficiency mutations are absent from every case arm.  (4) NOT exposed in AI context: formatCharacter5e (character-context.ts lines 77-90) formats proficiencies FOR READING (displays them to the AI) but provides no mutation interface. The AI sees "Skill Proficiencies: Athletics (expertise), Perception" but cannot issue a StatChange to grant/revoke them.  (5) NOT documented to model: dm-actions-schema.ts lines 162-166 document only "set_ability_score", "grant_feature", "revoke_feature" — no proficiency actions.  The gap is real and material. The engine persists proficiency state and the AI can observe it, but has no mutation mechanism to trigger capability-changes mid-session (e.g., a curse that revokes expertise, a boon that grants proficiency in a skill).
- **Wire-up sketch:** Add two StatChange variants (sketch): - grant_skill_proficiency: {characterName, skill, hasExpertise?, reason} - revoke_skill_proficiency: {characterName, skill, reason} - grant_save_proficiency: {characterName, ability, reason} - revoke_save_proficiency: {characterName, ability, reason}  Then wire into stat-mutations.ts applyChange (mutate char.skills[] proficient/expertise booleans and char.proficiencies.savingThrows array), add schemas to ai-schemas.ts and dm-actions-schema.ts prompt section.  Value is high because proficiency grants/revokes are thematically common (curse mechanics, magical transformation, blessings, class-feature awarding mid-session).

### G40 [high] <map-initiative> Wall segment creation, deletion, and modification (solid/door/window/one-way walls, isOpen state, oneWayDirection)
- **Engine + AI gap:** Engine implements WallSegment interface (src/renderer/src/types/map.ts:218-230) with full type/state support, plus store actions addWallSegment/removeWallSegment/updateWallSegment (src/renderer/src/stores/game/map-token-slice.ts:254-276). Walls are used for cover calculation (state-snapshot.ts:122). AI DM has zero DM action schema, union variant, or executor case for wall manipulation, and walls are never exposed in the state snapshot text. Prompt documentation (dm-actions-schema.ts) contains no wall action reference.
- **Wire-up sketch:** Create DmAction union variant for wall manipulation with three actions: (1) add_wall_segment {mapId?, x1, y1, x2, y2, type: 'solid'|'door'|'window'|'one-way'|'transparent', isOpen?, oneWayDirection?}; (2) remove_wall_segment {mapId?, wallId}; (3) update_wall_segment {mapId?, wallId, updates: Partial<WallSegment>}. Add Zod schemas and wire into DM_ACTION_SCHEMAS map. Implement executor cases dispatching to store.addWallSegment/removeWallSegment/updateWallSegment. Expose active map walls in [GAME STATE] snapshot under new "Walls:" section (listing type, coordinates, state). Document actions in dm-actions-schema.ts under new "Map Objects" subsection with examples (door ajar, one-way wall placement). Optionally: add AI context in combat-rules.ts or exploration-rules.ts so AI understands it can dynamically adjust terrain.

### G41 [high] <map-initiative> Terrain cell creation, deletion, and configuration (terrain type: difficult/hazard/water/climbing/portal, movement cost, hazard damage, portal targets, floor index)
- **Engine + AI gap:** Engine implements TerrainCell type with all requested properties and GameMap.terrain array. The map store's updateMap() function can mutate terrain via Partial<GameMap>. However, the AI DM has ZERO DM actions (no schemas, no executor cases, no context visibility) to trigger terrain manipulation. The game state snapshot never includes terrain cells, the prompt schema section documents terrain in combat rules but provides no actions to create/modify it, and the StatChange union has no terrain mutation types. The contract test prevents any action from being documented without an executor implementation, creating a hard barrier.
- **Wire-up sketch:** Add terrain manipulation DM actions: 1. Define schemas in ai-schemas.ts (add_terrain_cell, remove_terrain_cell, update_terrain_cell with type/cost/hazard/portal parameters) 2. Define union types in dm-actions.ts 3. Implement executors in game-actions/terrain-actions.ts (new module) calling gameStore.updateMap() 4. Export executor imports in game-action-executor.ts and add switch cases 5. Document in prompt-sections/dm-actions-schema.ts with examples 6. Update state-snapshot.ts to include terrain cells in [GAME STATE] block (with visual grid overlay description for AI spatial reasoning) 7. Ensure contract test passes by verifying schema↔executor bidirectional coverage

### G42 [high] <map-initiative> Drawing/annotation placement on maps (freehand lines, rectangles, circles, text labels with color, stroke width, visibility control, floor layer support)
- **Engine + AI gap:** Engine has full implementation (DrawingData type, addDrawing/removeDrawing/clearDrawings mutations in drawing-slice.ts). AI DM has zero access: no DM action schemas, no union variants, no executor cases, no snapshot context, no prompt documentation. Drawing state persists in the map but AI cannot manipulate it.
- **Wire-up sketch:** Add drawing DM actions: (1) Create DrawingSchema variants in ai-schemas.ts for each tool type (free, line, rect, circle, text with color/stroke/visibility/floor fields). (2) Add DmAction union cases: add_drawing, remove_drawing, clear_drawings. (3) Add executor handlers calling store addDrawing/removeDrawing/clearDrawings. (4) Expose activeMap.drawings in state-snapshot.ts context block. (5) Document in dm-actions-schema.ts prompt section with examples of annotating hazard zones, measurements, DM-only tactical notes.

### G43 [high] <map-initiative> Scene region/trigger zone creation with spatial shapes (circle/polygon/rectangle), triggers (enter/leave/start-turn/end-turn), and actions (alert-dm, teleport, apply-condition)
- **Engine + AI gap:** Engine has full SceneRegion type, store slice with CRUD operations (addRegion, removeRegion, updateRegion, clearRegions), and region data persists in GameMap. AI DM has zero DM actions for region management (63 actions checked in DM_ACTION_SCHEMAS; none for regions), no region context in state-snapshot.ts, and no prompt documentation. The schema↔executor contract test would catch partial implementations, confirming this is not a hidden/partial implementation.
- **Wire-up sketch:** Add region management DM actions to ai-schemas.ts (add_region, update_region, remove_region, enable_region, disable_region with shape/trigger/action parameters); add corresponding DmAction union variants to dm-actions.ts; implement executor cases in game-action-executor.ts dispatching to a new regions-actions.ts module; add region state to state-snapshot.ts context (active regions on map with IDs/names/shapes); document in dm-actions-schema.ts prompt section with region coordinate/shape examples.

### G44 [high] <map-initiative> Token elevation and floor layer assignment (elevation in feet with falling damage rules, multi-floor map support)
- **Engine + AI gap:** Engine implementation confirmed: MapToken.elevation and .floor fields exist (map.ts lines 101-103), updateToken auto-triggers falling damage logic for drops >= 10ft (map-token-slice.ts lines 191-206), and floor is set on token placement (line 303). AI DM gap confirmed: place_token/update_token/move_token schemas lack elevation/floor parameters (ai-schemas.ts), state snapshot never exposes elevation/floor data (state-snapshot.ts line 30 shows only gridX/gridY), and prompt documentation does not mention these capabilities (dm-actions-schema.ts lines 39-44). Token-actions.ts executors do not handle elevation/floor in create/update flows.
- **Wire-up sketch:** Add elevation/floor parameters to PlaceTokenSchema, UpdateTokenSchema, and MoveTokenSchema in ai-schemas.ts. Extend token description in state-snapshot.ts to include elevation and floor index. Document elevation/floor in dm-actions-schema.ts prompt section. Update executePlaceToken/executeUpdateToken/executeMoveToken in token-actions.ts to accept and apply elevation/floor updates. Add elevation/floor fields to the DmAction discriminated union in dm-actions.ts.

### G45 [high] <map-initiative> Token visual customization (color, border color, border style, label font size, aura with radius/color/opacity/visibility)
- **Engine + AI gap:** The MapToken interface fully implements these properties (src/renderer/src/types/map.ts lines 155-173), and the store's updateToken method accepts Partial<MapToken> which would apply any of them via spread. However, the AI DM cannot trigger any of this: (1) no DM action schema supports color/borderColor/borderStyle/labelFontSize/aura fields in place_token or update_token (ai-schemas.ts confirms update_token only has hp/ac/conditions/visibleToPlayers/label_new), (2) no stat change type exists for token visual mutations, (3) the state snapshot never exposes aura or visual styling to the AI context, (4) the prompt sections document only the 5 allowed update_token fields. The token-actions executor (lines 104-125) only applies the 5 explicitly handled fields, so even if an AI somehow sent color fields, they'd be silently ignored. The gap is confirmed: real engine capability, zero AI access.
- **Wire-up sketch:** To close this gap, add a new DM action 'customize_token' with fields for color, borderColor, borderStyle, labelFontSize, and aura; add schemas in ai-schemas.ts and dm-actions.ts; add an executor case in game-action-executor.ts that calls updateToken with the visual fields; optionally expose aura/color state in the snapshot context so the AI knows what's already customized. This would allow the AI to give tokens custom colors for factions, border styles for status (glowing borders for cursed tokens), and aura rings for spellcaster concentration or divine presence.

### G46 [high] <map-initiative> Token mobility and special movement (swim speed, climb speed, fly speed configuration; special senses: blindsight, tremorsense, truesight with range)
- **Engine + AI gap:** The engine implements MapToken.swimSpeed, climbSpeed, flySpeed, and specialSenses (src/renderer/src/types/map.ts lines 137-143), with full store mutation support via updateToken. However, the AI DM lacks: (1) no DM action fields for these speeds/senses in place_token/update_token/place_creature (src/main/ai/ai-schemas.ts + dm-actions.ts); (2) no StatChange union type for creature mobility (src/main/ai/ai-schemas.ts lines 272-303); (3) zero visibility in state-snapshot.ts context (confirmed via grep: no swimSpeed/climbSpeed/flySpeed/specialSenses references); (4) zero mention in prompt sections (src/main/ai/prompt-sections/dm-actions-schema.ts documents only walk speed). Contract test (ai-schemas.test.ts) confirms all schema↔executor pairs exist, but these properties have neither.
- **Wire-up sketch:** 1. Add fields to DM_ACTION_SCHEMAS: extend place_token/update_token schemas with optional swimSpeed, climbSpeed, flySpeed, specialSenses; add place_creature support 2. Add DmAction union variants reflecting new fields 3. Update executeUpdateToken and executePlaceToken in token-actions.ts to map these fields to Partial<MapToken> 4. Append movement/senses to state-snapshot.ts token line (line 30+): report non-zero speeds + special senses for each token 5. Update dm-actions-schema.ts prompt (lines 40-44) to document new action fields with examples (e.g., `swimSpeed: 30, specialSenses: [{type: 'blindsight', range: 60}]`) 6. Update place_creature library code (token-placement.ts) to extract swim/climb/fly from creature.speed object and populate token 7. Add prompt section guiding terrain interaction (e.g., "creatures with swim speed ignore water movement penalties") 8. Add regression test case to ai-schemas.test.ts validating a place_creature with special senses passes schema

### G47 [high] <map-initiative> Token damage resistance/vulnerability/immunity configuration and reporting
- **Engine + AI gap:** The engine genuinely implements token resistances/vulnerabilities/immunities (MapToken fields exist, updateToken can mutate them, and creature-mutations.ts handles the stat changes correctly). The AI DM genuinely has DM actions and stat changes to SET these properties (creature_set_resistance/vulnerability/immunity in ai-schemas.ts + types.ts + prompt sections). However, the AI CANNOT PERCEIVE these properties when making combat decisions. The buildActiveCreatures() function omits resistances/vulnerabilities/immunities from ActiveCreatureInfo, the state-snapshot doesn't report token-level resistances, and formatCreatureContext() only extracts them from stat blocks—not from the token fields set by the AI's own mutations. This means the AI can set a creature's fire resistance but won't see it in the next decision cycle, breaking tactical reasoning about damage mitigation.
- **Wire-up sketch:** 1. Add resistances, vulnerabilities, and immunities fields to the ActiveCreatureInfo interface (src/main/ai/types.ts). 2. Update buildActiveCreatures() in ai-dm-routing.ts to extract these three fields from tokens: `resistances: t.resistances ?? [], vulnerabilities: t.vulnerabilities ?? [], immunities: t.immunities ?? []`. 3. Update formatCreatureContext() in context-builder.ts to report these three lists from the creature info (in addition to the stat-block-sourced ones) so the AI sees both static stat-block resistances and dynamically-set token resistances. 4. Optionally: add these to the game state snapshot's token listing (state-snapshot.ts) so the AI can reason about creature defenses even without a stat block lookup.
