/**
 * Phase 34a — translation key type. A stub (`string`) until the 34k generator
 * derives a literal union from `en.json`, which will make `t()` keys type-safe.
 */
export type TranslationKeys = string
